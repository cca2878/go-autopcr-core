# 构建产物
/bin/
/dist/
*.exe

# Go
/vendor/
*.test
*.out
go.work
go.work.sum

# 参考项目：原 Python 项目、设计文档、兄弟仓库与测试凭据，只留本地做交叉核对，不属于本仓。
/ref/

# 运行时缓存（per-version 干净母数据库等成品，运行时生成）
/cache/

# Python 虚拟环境（仅本地工具链使用）
/.venv/

# 本地专属的配置/文件（含 .claude/settings.local.json 等）
*.local.json
.DS_Store
